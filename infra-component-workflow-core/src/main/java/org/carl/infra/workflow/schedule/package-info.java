/**
 * Runtime-independent contracts for scheduling recurring workflow executions.
 *
 * <p>The models in this package contain only JDK and workflow-core types. Runtime adapters map
 * them to a concrete durable scheduler such as Temporal Schedules.
 */
package org.carl.infra.workflow.schedule;
