/**
 * Immutable wire-model records for workflow definitions and execution results.
 *
 * <p>This package intentionally contains no runtime logic. The DSL builds these records, graph and
 * engine helpers read them, and runtime adapters persist or replay them.
 */
package org.carl.infrastructure.workflow.definition;
